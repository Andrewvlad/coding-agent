[project]
name = "coding-agent"
version = "0.1.0"
description = "Testing out rolling your own agentic AI with calls to Gemini"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
